# Build stage
FROM node:20-slim AS builder

WORKDIR /app

# Copy package files
COPY armor_select/frontend/package*.json ./
RUN npm ci

# Copy source files
COPY armor_select/frontend/ ./

# Build the app
RUN npm run build

# Production stage
FROM nginx:alpine

# Copy built files from builder
COPY --from=builder /app/dist /usr/share/nginx/html

# Copy nginx configuration
COPY armor_select/docker/nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
